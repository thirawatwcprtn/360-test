# 🎯 คู่มือการรัน Tests แบบละเอียด

## 📚 สารบัญ
1. [การเตรียมพร้อม](#การเตรียมพร้อม)
2. [การติดตั้งครั้งแรก](#การติดตั้งครั้งแรก)
3. [การรัน Tests พื้นฐาน](#การรัน-tests-พื้นฐาน)
4. [การรัน Tests ขั้นสูง](#การรัน-tests-ขั้นสูง)
5. [การดูผลลัพธ์](#การดูผลลัพธ์)
6. [การแก้ไขปัญหา](#การแก้ไขปัญหา)

---

## การเตรียมพร้อม

### ✅ สิ่งที่ต้องมีก่อนเริ่ม

1. **ติดตั้งซอฟต์แวร์เหล่านี้:**
   - Node.js เวอร์ชัน 18 ขึ้นไป
   - pnpm (ติดตั้งด้วย `npm install -g pnpm`)
   - Git

2. **ตรวจสอบว่าติดตั้งแล้ว:**
```bash
node --version    # ควรแสดง v18.x.x หรือสูงกว่า
pnpm --version    # ควรแสดง 8.x.x หรือสูงกว่า
```

3. **เปิด Services ที่จำเป็น:**
   - Backend API (port 3001)
   - Web Backoffice (port 8000)
   - Database (PostgreSQL)

---

## การติดตั้งครั้งแรก

### 📦 ขั้นตอนที่ 1: เข้าโฟลเดอร์ test

```bash
# เปิด Terminal/Command Prompt
cd employee-survey-platform/test
```

### 📥 ขั้นตอนที่ 2: ติดตั้ง Dependencies

```bash
# ติดตั้ง package ทั้งหมด
pnpm install
```

รอจนกว่าจะเสร็จ (ประมาณ 1-2 นาที)

### 🌐 ขั้นตอนที่ 3: ติดตั้ง Browsers

```bash
# ติดตั้ง browsers สำหรับ Playwright
pnpm install:browsers
```

**หมายเหตุ:** คำสั่งนี้จะดาวน์โหลด Chromium, Firefox, และ WebKit (ประมาณ 500MB)

### ⚙️ ขั้นตอนที่ 4: ตั้งค่า Environment

```bash
# คัดลอกไฟล์ตัวอย่าง
cp env.example .env

# แก้ไขไฟล์ .env (ใช้ text editor ที่ชอบ)
# macOS/Linux:
nano .env

# Windows:
notepad .env
```

**ตัวอย่างไฟล์ .env:**
```bash
NODE_ENV=development
API_BASE_URL=http://localhost:3001
WEB_BASE_URL=http://localhost:8000
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin1235
```

### ✔️ ขั้นตอนที่ 5: ตรวจสอบการติดตั้ง

```bash
# ตรวจสอบว่าทุกอย่างพร้อม
pnpm validate:env
```

ถ้าไม่มี error แสดงว่าพร้อมใช้งาน! ✅

---

## การรัน Tests พื้นฐาน

### 🚀 วิธีที่ 1: รัน Test ทั้งหมด

```bash
# รัน test ทุกตัว
pnpm test
```

**ผลลัพธ์ที่ได้:**
```
Running 217 tests using 4 workers
  125 passed (2m)
  [chromium] › tests/api/auth.api.test.ts:10:5 › should login with valid credentials
  ✓ [chromium] › tests/ui/auth.ui.test.ts:15:5 › should display login form
  ...
```

### ⚡ วิธีที่ 2: รัน Test ที่เพิ่มใหม่

```bash
# รันเฉพาะ test ที่เพิ่มใหม่
pnpm test:all-new
```

### 👀 วิธีที่ 3: รันแบบเห็น Browser (แนะนำสำหรับมือใหม่)

```bash
# รันแบบเห็นหน้า browser
pnpm test:headed
```

คุณจะเห็น browser เปิดขึ้นมาและทำงานอัตโนมัติ

### 🐛 วิธีที่ 4: รันแบบ Debug

```bash
# รันแบบ debug mode
pnpm test:debug
```

จะเปิด Playwright Inspector ให้ควบคุม test ทีละขั้นตอน

---

## การรัน Tests ขั้นสูง

### 📋 รันตามประเภท Test

#### 1. API Tests (เร็วที่สุด)
```bash
# รัน API tests ทั้งหมด
pnpm test:api

# ผลลัพธ์: รัน ~125 tests ใช้เวลาประมาณ 1-2 นาที
```

#### 2. UI Tests
```bash
# รัน UI tests ทั้งหมด
pnpm test:ui

# ผลลัพธ์: รัน ~80 tests ใช้เวลาประมาณ 3-5 นาที
```

#### 3. E2E Tests
```bash
# รัน E2E tests ทั้งหมด
pnpm test:e2e

# ผลลัพธ์: รัน ~5 tests ใช้เวลาประมาณ 2-3 นาที
```

#### 4. Integration Tests
```bash
# รัน Integration tests ทั้งหมด
pnpm test:integration

# ผลลัพธ์: รัน ~7 tests ใช้เวลาประมาณ 5-10 นาที
```

---

### 🎯 รันตามฟีเจอร์

#### 🔐 Authentication (การเข้าสู่ระบบ)

```bash
# รัน authentication tests ทั้งหมด
pnpm test:auth

# รันเฉพาะ API
pnpm test:api:auth

# รันเฉพาะ UI
pnpm test:ui:auth
```

**ทดสอบอะไรบ้าง:**
- การ login/logout
- การตรวจสอบ token
- ความปลอดภัย (XSS, SQL Injection)
- Session management

---

#### 👥 Employee Management (จัดการพนักงาน)

```bash
# รัน employee tests ทั้งหมด
pnpm test:employee

# รันเฉพาะ API (CRUD, Bulk operations)
pnpm test:api:employee

# รันเฉพาะ UI (Forms, Lists)
pnpm test:ui:employee

# รันเฉพาะ E2E workflows
pnpm test:e2e:employee
```

**ทดสอบอะไรบ้าง:**
- สร้าง/แก้ไข/ลบพนักงาน
- Bulk create/update/delete
- ค้นหาและกรองข้อมูล
- Import/Export CSV
- การแสดงผลหลายภาษา

---

#### 📝 Survey Response (ตอบแบบสอบถาม)

```bash
# รัน survey response tests ทั้งหมด
pnpm test:survey-response

# รันเฉพาะ API
pnpm test:api:survey-response

# รันเฉพาะ UI
pnpm test:ui:survey-response
```

**ทดสอบอะไรบ้าง:**
- การตอบแบบสอบถาม
- Validation ของคำตอบ
- Conditional questions
- Multi-locale (ไทย/อังกฤษ)
- Token-based access

---

#### 📊 Reporting (รายงาน)

```bash
# รัน reporting tests ทั้งหมด
pnpm test:reporting

# รันเฉพาะ API
pnpm test:api:reporting

# รันเฉพาะ UI
pnpm test:ui:reporting
```

**ทดสอบอะไรบ้าง:**
- Export ข้อมูล (CSV, Excel, PDF)
- สถิติและ analytics
- Dashboard
- Charts และ graphs
- Report generation

---

### ⚡ การรันแบบเร็ว

```bash
# รัน API tests แบบขนาน (เร็วที่สุด)
pnpm test:quick

# รัน test ทั้งหมดแบบขนาน
pnpm test:parallel
```

---

### 🌍 รันตาม Environment

```bash
# Development
pnpm test:dev

# Staging
pnpm test:staging

# Production
pnpm test:prod
```

---

## การดูผลลัพธ์

### 📊 วิธีที่ 1: ดูใน Terminal

เมื่อรัน test เสร็จจะแสดงผลแบบนี้:

```
✓ [chromium] › auth.api.test.ts:15:5 › should login with valid credentials (123ms)
✓ [chromium] › auth.api.test.ts:25:5 › should fail with invalid credentials (89ms)
✗ [chromium] › employee.ui.test.ts:45:5 › should create employee (1.2s)

  2 passed (212ms)
  1 failed
```

- ✓ = ผ่าน (passed)
- ✗ = ล้มเหลว (failed)

### 📈 วิธีที่ 2: เปิด HTML Report (แนะนำ)

```bash
# เปิด HTML report
pnpm test:report
```

จะเปิด browser แสดง:
- รายการ test ทั้งหมด
- เวลาที่ใช้
- Screenshots เมื่อ test ล้มเหลว
- Video recordings
- Trace files สำหรับ debug

### 📁 วิธีที่ 3: ดูไฟล์ Report

```bash
# ตำแหน่งไฟล์ reports
test/
├── playwright-report/        # HTML Report (เปิดด้วย browser)
│   └── index.html
├── test-results/            # ผลลัพธ์แต่ละ test
│   ├── results.json        # JSON format
│   ├── results.xml         # JUnit format (CI/CD)
│   └── screenshots/        # รูปภาพหน้าจอ
```

---

## การแก้ไขปัญหา

### ❌ ปัญหา 1: "ECONNREFUSED" - ติดต่อ server ไม่ได้

**สาเหตุ:** Backend API หรือ Web Backoffice ไม่ได้รันอยู่

**วิธีแก้:**

```bash
# 1. ตรวจสอบว่า services รันอยู่หรือไม่
curl http://localhost:3001/_docs    # Backend API
curl http://localhost:8000          # Web Backoffice

# 2. ถ้าไม่รัน ให้เปิดใน terminal ใหม่

# เปิด Backend API
cd employee-survey-platform/backend-api
pnpm start:dev:app

# เปิด Web Backoffice (terminal ใหม่)
cd employee-survey-platform/web-backoffice
pnpm start:dev
```

---

### ❌ ปัญหา 2: "Browser not found"

**สาเหตุ:** ยังไม่ได้ติดตั้ง browsers

**วิธีแก้:**

```bash
cd test
pnpm install:browsers
```

---

### ❌ ปัญหา 3: "Authentication failed"

**สาเหตุ:** Username/Password ใน .env ไม่ถูกต้อง

**วิธีแก้:**

```bash
# 1. ตรวจสอบ .env
cat .env | grep ADMIN

# 2. แก้ไขให้ตรงกับ database
nano .env

# 3. ทดสอบ login ด้วย curl
curl -X POST http://localhost:3001/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin1235"}'
```

---

### ❌ ปัญหา 4: Test timeout

**สาเหตุ:** Server ตอบกลับช้า

**วิธีแก้:**

```bash
# แก้ไขไฟล์ playwright.config.ts
timeout: 60000,  # เพิ่มเป็น 60 วินาที
```

---

### ❌ ปัญหา 5: Port ถูกใช้งานแล้ว

**สาเหตุ:** มี process อื่นใช้ port อยู่

**วิธีแก้ (macOS/Linux):**

```bash
# หา process ที่ใช้ port
lsof -i :3001
lsof -i :8000

# ปิด process
kill -9 <PID>
```

**วิธีแก้ (Windows):**

```bash
# หา process
netstat -ano | findstr :3001
netstat -ano | findstr :8000

# ปิด process
taskkill /PID <PID> /F
```

---

## 💡 เคล็ดลับการใช้งาน

### 1. รัน Test เฉพาะไฟล์

```bash
# รันเฉพาะไฟล์ auth.api.test.ts
pnpm test tests/api/auth.api.test.ts

# รันเฉพาะไฟล์ employee.ui.test.ts
pnpm test tests/ui/employee.ui.test.ts
```

### 2. รัน Test เฉพาะที่มีชื่อตรงกัน

```bash
# รันเฉพาะ test ที่มีคำว่า "login"
pnpm test -- -g "login"

# รันเฉพาะ test ที่มีคำว่า "create employee"
pnpm test -- -g "create employee"
```

### 3. รันบน Browser เฉพาะตัว

```bash
# รันบน Chrome เท่านั้น
pnpm test -- --project=chromium

# รันบน Firefox เท่านั้น
pnpm test -- --project=firefox

# รันบน Safari เท่านั้น
pnpm test -- --project=webkit
```

### 4. รัน Test ที่ล้มเหลวอีกครั้ง

```bash
# รันเฉพาะ test ที่ล้มเหลวครั้งก่อน
pnpm test -- --last-failed
```

### 5. ดู Test แบบ Slow Motion

```bash
# รันแบบเห็นและชะลอการทำงาน
pnpm test:headed -- --slow-mo=1000
```

---

## 📊 สรุปคำสั่งที่ใช้บ่อย

| คำสั่ง | ความหมาย | เวลาที่ใช้ |
|--------|----------|-----------|
| `pnpm test` | รัน test ทั้งหมด | 5-10 นาที |
| `pnpm test:all-new` | รัน test ใหม่ทั้งหมด | 5-10 นาที |
| `pnpm test:api` | รัน API tests | 1-2 นาที |
| `pnpm test:ui` | รัน UI tests | 3-5 นาที |
| `pnpm test:auth` | รัน Auth tests | 1-2 นาที |
| `pnpm test:employee` | รัน Employee tests | 2-3 นาที |
| `pnpm test:quick` | รันแบบเร็ว (API) | 1 นาที |
| `pnpm test:headed` | รันแบบเห็น browser | 5-10 นาที |
| `pnpm test:debug` | รันแบบ debug | ตามต้องการ |
| `pnpm test:report` | เปิด report | ทันที |

---

## 📋 Checklist การรัน Test

### ก่อนรัน Test ✅

- [ ] Services รันอยู่ (Backend API, Web Backoffice)
- [ ] Database รันอยู่และมีข้อมูล
- [ ] ไฟล์ .env ตั้งค่าถูกต้อง
- [ ] ติดตั้ง dependencies แล้ว (`pnpm install`)
- [ ] ติดตั้ง browsers แล้ว (`pnpm install:browsers`)

### หลังรัน Test ✅

- [ ] ตรวจสอบผลลัพธ์ใน terminal
- [ ] เปิด HTML report ดูรายละเอียด
- [ ] ตรวจสอบ screenshots ถ้า test ล้มเหลว
- [ ] แก้ไข code ถ้าจำเป็น
- [ ] รัน test ใหม่อีกครั้ง

---

## 🎓 ขั้นตอนสำหรับมือใหม่

### Day 1: เริ่มต้น

```bash
# 1. ติดตั้งทุกอย่าง
cd test
pnpm install
pnpm install:browsers
cp env.example .env

# 2. รัน test แรก
pnpm test:auth

# 3. ดูผลลัพธ์
pnpm test:report
```

### Day 2: ทดลองรัน

```bash
# รัน test แต่ละประเภท
pnpm test:api              # ทดสอบ API
pnpm test:ui               # ทดสอบ UI
pnpm test:e2e              # ทดสอบ E2E
```

### Day 3: เรียนรู้การ Debug

```bash
# รันแบบเห็น browser
pnpm test:headed

# รันแบบ debug
pnpm test:debug
```

### Day 4: รัน Test ทั้งหมด

```bash
# รัน test ทั้งหมด
pnpm test

# ดู report
pnpm test:report
```

---

## 📞 ช่วยเหลือ

หากยังมีปัญหา:

1. อ่าน **[INTRODUCTION.md](./INTRODUCTION.md)** สำหรับข้อมูลเพิ่มเติม
2. อ่าน **[NEW_TESTS_GUIDE.md](./NEW_TESTS_GUIDE.md)** สำหรับรายละเอียด
3. ตรวจสอบ [Playwright Documentation](https://playwright.dev)
4. ติดต่อทีมพัฒนา

---

## 🎉 สำเร็จ!

ตอนนี้คุณพร้อมรัน tests แล้ว! 🚀

เริ่มต้นด้วย:
```bash
cd test
pnpm test:auth
pnpm test:report
```

**Happy Testing! 🧪**

---

*อัปเดตล่าสุด: December 2024*
*สร้างด้วย ❤️ โดย Claude Code*
